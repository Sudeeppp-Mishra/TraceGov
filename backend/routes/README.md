# Backend Routes

This directory contains route configuration files mapping specific API endpoints to their controller handler actions.

## Folder Contents
Organize routes by resource:
- `authRoutes.js` (for registration and login endpoints)
- `documentRoutes.js` (for managing and scanning files)
