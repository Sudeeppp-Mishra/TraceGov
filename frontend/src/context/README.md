# Frontend Context

Application level state contexts.

## Expected Contexts
- `AuthContext.jsx`: Manages global user authentication status, role tokens, login, and logout functions.
- `ThemeContext.jsx` (optional): App-wide visual styling contexts.
